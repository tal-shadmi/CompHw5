void fallthrough(int x) {
    switch (x) {
            case 1: printi(1); break;
            case 2: printi(2);
            default: printi(0);
            case 3: printi(3);
    }
}


void main() {
	fallthrough(1);
    fallthrough(2);
    fallthrough(3);
    fallthrough(4);
}